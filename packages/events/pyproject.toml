[project]
name = "krishimitra-events"
version = "0.1.0"
description = "Event publishing, consuming, and outbox support for KrishiMitra-AI."
requires-python = ">=3.13,<3.14"
dependencies = [
    "pydantic>=2.11.7,<3.0.0",
    "krishimitra-contracts",
]

[tool.uv]
package = true

[tool.uv.sources]
krishimitra-contracts = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/krishimitra_events"]